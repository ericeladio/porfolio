---
const { data } = Astro.props;
---

<section>
    <header class="title" id="projects">Proyectos</header>
    {
        data.map((item) => (
            <div class="container">
                <div class="info-container">
                    <img
                        class="image-proyect"
                        alt="image_proyect"
                        src="https://blog.openclassrooms.com/es/wp-content/uploads/sites/5/2017/09/AdobeStock_126016889apaisado-710x367.jpg"
                    />
                </div>
                <div class="info-container info">
                    <h3 class="title-proyect">{item.title}</h3>
                    <div>
                        <img alt="icon" />
                    </div>
                    <p class="description">{item.description}</p>
                    <div>
                        <button class="button">preview</button>
                    </div>
                </div>
            </div>
        ))
    }
</section>
<style>
    .container {
        display: flex;
        align-items: center;
    }
    .container div {
        flex: 1;
        margin: 30px 0;
    }
    .image-proyect {
        width: 450px;
        height: 250px;
        border-radius: 15px;
    }
    .info-container {
        width: 450px;
        height: 250px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .info {
        display: block;
    }
    .description {
        width: 80%;
        display: inline-block;
    }
    .title {
        color: #051937;
        font-size: 40px;
    }
    .title-proyect {
        font-size: 25px;
    }
    .button {
        background-color: #051937;
        color: azure;
        border-radius: 15px;
        padding: 8px 45px;
        border: none;
    }
    @media screen and (max-width: 1000px) {
        .container {
            flex-direction: column;
        }

        .title {
            margin-left: 40px;
        }
        @media screen and (max-width: 730px) {
            .info-container {
                width: 90%;
            }
        }
        .image-proyect {
            width: 100%;
            height: 250px;

        }
    }
</style>ß
